<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Silver Admin</title>
</head>
<body>
    <div>
        <header>
            <h1 class="flex justify-center text-3xl font-bold m-10" >Admin Panel</h1>
        </header>
        <section>
<div class="flex flex-wrap gap-2">
    <%products.forEach((product)=>{%>
        <% if(products.length>0){ %>
            <div class="product flex w-full p-3 rounded-lg bg-slate-200">
                <div class=" h-28 w-28 rounded-lg bg-zinc-500 overflow-hidden">
                    <img src="<%=product.image%>" alt="image" class="w-full h-full object-cover object-center">
                </div>
                <h3 class="text-lg px-5"><%= product.name%></h3>
                <h3 class="text-lg px-5"><%= product.description%></h3>
                <h3 class="text-lg px-5"><%= product.seller%></h3>
                <h3 class="text-lg px-5"><%= product.price%></h3>
                <h3 class="text-lg px-5"><%= product.discount%></h3>
                <h3 class="text-lg px-5"><%= product.stock%></h3>
                <h3 class="text-lg px-5"><%= product.category%></h3>
             
                    <div class="">
                        <a class="text-slate-400" href="/edit/<%= product._id%>">Edit</a>
                        <a class="text-red-500" href="/delete/<%= product._id%>">Delete</a>
                    </div>
                
            </div>
            <%   } else {%>
                <h2 class="text-2xl text-zinc-500">No Products yet</h2>
                  <%  }%>
    <%})%>
</div>
        </section>
    </div>

</body>
</html>